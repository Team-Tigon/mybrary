<h1>Listing items</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @items.each do |item| %>
      <tr>
        <td><%= link_to item.name, user_items_path(item) %></td>
        <td><%= link_to 'Edit', edit_user_item_path(item) %></td>
        <td><%= link_to 'Destroy', user_items_path %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New item', new_user_item_path %>
